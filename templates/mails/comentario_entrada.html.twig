<link href="{{ asset('vendor/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
<script src="{{ asset('vendor/jquery/jquery.min.js') }}"></script>
<script src="{{ asset('vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
<div class="row">
    <div>
        <h5>Estimado Usuario.</h5> 
        <p>
            Informamos que el administrador a comentado su 
            {% if tipo == 1 %}
                noticia
            {% elseif tipo == 2 %}
                 programa
            {% elseif  tipo == 3 %}
                 evento en la agenda
            {% endif %}
            con el fin de que realize los cambios solicitados, una vez efectuados la publicación será revisada nuevamente para su aprobación.
            <br>
            <strong>Titulo: </strong> {{entrada.titulo}}
            <br> 
            {% if tipo == 1 %}
                <strong>URL: </strong><a href="{{url('index')}}backend/?entity=Noticia&action=show&id={{entrada.id}}">{{url('index')}}backend/?entity=Noticia&action=show&id={{entrada.id}}</a>
             {% elseif tipo == 2 %}
                <strong>URL: </strong><a href="{{url('index')}}backend/?entity=Programa&action=show&id={{entrada.id}}">{{url('index')}}backend/?entity=Noticia&action=show&id={{entrada.id}}</a>
            {% elseif  tipo == 3 %}
                <strong>URL: </strong><a href="{{url('index')}}backend/?entity=Agenda&action=show&id={{entrada.id}}">{{url('index')}}backend/?entity=Noticia&action=show&id={{entrada.id}}</a>
            {% endif %}    
            <br>
            Se realizaron los siguientes comentarios:
                <br>            
           
            <ul>
                {% for comentario in nuevos_comentarios %}
                    <li>
                        {{comentario.descripcion}}
                    </li>              
                {% endfor %}
            </ul>
        </p>
        <br>
        Ante cualquier consulta o duda comuniquese a 
            {% if soporte_mail.contenido is defined%}
                {{soporte_mail.contenido}}
            {% else %}
                 {{soporte_mail}}
            {% endif %} y telefónicamente al 
            {% if soporte_tel.contenido is defined%}
                {{soporte_tel.contenido}}
            {% else %}
                 {{soporte_tel}}
            {% endif %}
        <br>
        <div style="text-align:center;width:20%;">
            Atte. <br>
            Equipo de administración ONG<br>
        </div>
        <div style="width:50%;">
            <img  class="img-fluid" src="{{url('index')}}/img/logos_head.png">
        </div>
    </div>
</div>